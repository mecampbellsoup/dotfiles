# pyenv
if command -v pyenv &> /dev/null
then
  export PYENV_ROOT="$HOME/.pyenv"
  [[ -d $PYENV_ROOT/bin ]] && export PATH="$PYENV_ROOT/bin:$PATH"
  eval "$(pyenv init -)"
fi

# Added by `rbenv init` on Fri Jun  7 01:27:13 PM EDT 2024
if command -v rbenv &> /dev/null
then
  eval "$(rbenv init - zsh)"
fi

